name: JavaScript

# permissions:
#  pull-requests: write
#  contents: write
#  id-token: write # This is required for requesting the JWT

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

jobs:
  javascript:
    uses: zaphiro-technologies/github-workflows/.github/workflows/js.yaml@main
    secrets: inherit
